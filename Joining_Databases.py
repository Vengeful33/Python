
#We can add databases together using the program below:

ListBool = [True, False, True]
ListNumber = [12, 13, 14]

FullList = ListBool + ListNumber
print(FullList)

#We can use also strings and floats, the first variable used in a plus list will be in front, and so forth. 