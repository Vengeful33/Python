
#We can create a list be using brackets []

Store = ["Milk", "Eggs", "Bacon"]
print(Store)

# We can create an empty list that has zero values

Users = []

#With lists there are indices and elements, elements are the values in the list, indices start at 0 and increase for every element, like so:

Classes = ["P.E", "History", "Science"]
print(Classes[1])

#This prints History since indices start at zero.

#We can use this to also change a value in a list

Time = [10, 12, 16]
Time[2] = 24
print(Time)

#This changes it to 10, 12, and 24